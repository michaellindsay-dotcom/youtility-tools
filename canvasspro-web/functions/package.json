{
  "name": "youtilityknock-functions",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Functions for YoutilityKnock: Knockstat proxy + company/account management.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "pdfkit": "^0.15.1",
    "stripe": "^22.2.0"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.13.5",
    "typescript": "^5.5.4"
  }
}
