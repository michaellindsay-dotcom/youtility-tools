# Copy to functions/.env (gitignored). The Knockstat API key lives ONLY here,
# server-side — it is never shipped to the browser or the mobile apps.
KNOCKSTAT_API_KEY=op_live_xxxxxxxxxxxxxxxxxxxx
KNOCKSTAT_BASE_URL=https://api.knockstat.com/v1
KNOCKSTAT_ENDPOINT=/property
